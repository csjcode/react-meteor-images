// Create image list component
// 3 necessary steps (improt, create, export))
// 1. Import react

import React from 'react';
import ReactDOM from 'react-dom';

// 2. Create Component

const imageList = () => {
   return (
   <ul>
   </ul>
   )
}

// 3. Export Component
